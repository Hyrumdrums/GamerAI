fastapi==0.115.0
uvicorn[standard]==0.30.6
httpx==0.27.2
# Required for FastAPI Form() parsing on POST /invite/<code> (the
# invitee redemption flow). Without this, the form submit 500s with
# RuntimeError: Form data requires "python-multipart" to be installed.
python-multipart==0.0.9
# Jinja2 templates live under client/templates/; FastAPI's
# Jinja2Templates wrapper hands them request context (url_for, etc.).
jinja2==3.1.4
