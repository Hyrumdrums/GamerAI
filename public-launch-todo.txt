GamerAI — Public Launch Todo
Generated from the 2026-08-19 launch-readiness review. Small, one-commit-sized
tasks, in the suggested order. Status legend: [ ] pending  [x] done+verified

--- Step 1: repo safety check ---------------------------------------------
[x] 1.1  SSH to VPS, confirm the GitHub deploy key (/root/.ssh/github_deploy)
         is a read-only per-repo Deploy Key, not a write-enabled key or a
         personal PAT. Fix in GitHub settings if wrong.
         verify: `gh api repos/Hyrumdrums/GamerAI/keys` shows
         "gamerai-github-deploy", read_only:true. Confirmed the VPS's actual
         key (`ssh-keygen -y -f /root/.ssh/github_deploy`) fingerprint-matches
         that exact registered key. No fix needed. (2026-08-19)

--- Step 2: scrub personal details -----------------------------------------
[x] 2.1  Replace the named family member ("Carol") in docs/devlog.md with a
         role label (e.g. "the head-machine operator"); keep all technical
         content unchanged.
         verify: `grep -in carol docs/*.md README.md` returns only the
         generic Alice/Bob/Carol invite-chain example in auth-design.md
         (not the real person) — devlog.md is clean. (2026-08-19)

--- Step 3: prompt size cap -------------------------------------------------
[x] 3.1  Add MAX_PROMPT_BYTES env-configurable cap on POST /generate in
         coordinator/main.py; reject oversized prompts with 413.
         verify: PromptSizeLimitTests in tests/test_coordinator_e2e.py —
         disabled-by-default, under-cap, over-cap, and UTF-8-byte-vs-char
         counting all covered. Full suite: 558/558 green. (2026-08-19)

--- Step 4: capacity ceiling -------------------------------------------------
[x] 4.1  Add a global in-flight-jobs ceiling on POST /generate tied to live
         worker count; return 503 with a friendly "network at capacity, try
         again shortly" message when exceeded, instead of an unbounded queue.
         Wired as CAPACITY_JOBS_PER_WORKER (0=disabled default, matches the
         RATE_LIMIT_PER_MIN convention) through docker-compose.yml and
         documented in README §11.
         verify: CapacityCeilingTests in tests/test_coordinator_e2e.py —
         disabled-by-default, ceiling hit with 1 worker, ceiling scales
         with worker count, completing a job frees capacity, zero-worker
         floor-of-1 so a cold network isn't locked out. Full suite:
         563/563 green. (2026-08-19)

--- Step 5: public signup ----------------------------------------------------
[x] 5.1  Add public POST /signup (no bearer, rate-limited like /login) that
         creates a BRONZE member with no parent, via a new atomic
         db.create_signup_member() (mirrors accept_invite_atomic's
         collision-checked insert), stamping current TOS_VERSION.
         Note: adjusted scope during build — invites in this system are
         pre-addressed to a specific invitee_email at creation time, so
         "your own invite code" to hand out like a coupon doesn't fit the
         data model. /signup instead returns role="contributor" (the
         exact role POST /invites already authorizes to create invites),
         so a new member can immediately call POST /invites themselves.
         Throttled per-IP (SIGNUP_MAX_PER_IP=5/SIGNUP_WINDOW_SECONDS=1h,
         counts successes only) rather than reusing RATE_LIMIT_PER_MIN,
         which is sized for the streaming-poll path and usually off.
         verify: SignupTests in tests/test_coordinator_e2e.py — token
         resolves to the right member, contributor role satisfies the
         /invites authorization check, ToS required, duplicate
         username/email 409, throttle blocks after N successful
         signups from one IP and does NOT count failed attempts. Full
         suite: 570/570 green. (2026-08-19)
[x] 5.2  Wire windows-agent first run: if the agent has no paired member
         token yet, offer to call /signup, persist the token to
         state.json, and print the new username/password to console.
         Note: adjusted scope during build — there's no per-member
         "invite link" to print (see 5.1's note); instead the agent
         prints the new account's username/password (shown once) so
         the contributor can sign into the chat UI and invite friends
         from there. Added a new `run_signup_flow()` (mirrors
         run_pair_flow's shape) + `--signup` CLI flag, and wired the
         real first-run gate (the `have_token` block in main(), NOT the
         unused `resolve_api_token()` prompt) to ask "already have an
         account? [y/N]" and route to --pair or --signup accordingly.
         Requires an explicit y/n ToS confirmation before calling the
         endpoint, matching the web redemption page's click-through bar.
         verify: RunSignupFlowTests in tests/test_agent_bootstrap.py
         (mocked httpx) — decline skips the call, acceptance persists
         the token to state.json and sends a well-formed signup body,
         coordinator rejection and network failure both fail closed
         without persisting. Full suite: 574/574 green. (2026-08-19)

--- Step 6: no-install demo mode ---------------------------------------------
[x] 6.1  Seed a single GUEST member at boot (ensure_guest_seed, same
         pattern as ensure_admin_seed) and add a public, tight per-IP
         rate limit (DEMO_MAX_PER_IP=5/hour default) on a demo chat
         path, plus a small shared daily-token backstop
         (DEMO_DAILY_TOKEN_CAP) against many-IP abuse. Built a real
         page, not just the backend: GET /demo (client/routes/demo.py +
         demo.html.j2) with an explicit "demo mode" banner and a link
         to download the agent, backed by coordinator POST /demo/generate
         + GET /demo/result/{job_id} (forces tool=chat, ignores any other
         field, reuses the real generate()/result() gates — capacity
         ceiling, live-worker check, prompt cap — by attaching the guest
         member to the request the same way real auth does).
         verify: DemoModeTests in tests/test_coordinator_e2e.py (backend:
         throttle blocks the 6th message and doesn't count 503s against
         it, daily-cap backstop, job ownership scoping, smuggled
         tool/model ignored, disabled->404) + 3 new cases in
         tests/test_web_ui_smoke.py (page renders anonymously, generate+
         result round-trips with no session/auth header). Full suite:
         587/587 green. (2026-08-19)

         Superseded (2026-08-20): the live worker-backed demo meant a
         public, no-account page could still burn real inference cost
         under sustained/distributed abuse — throttle + daily cap raise
         the bar but don't remove the cost. Replaced with a fully
         client-side canned-response library (demo.html.j2); no request
         ever reaches the coordinator. GUEST_MEMBER_ID, ensure_guest_seed,
         POST /demo/generate, GET /demo/result/{job_id}, and the
         DEMO_ENABLED/DEMO_MAX_PER_IP/DEMO_WINDOW_SECONDS/
         DEMO_DAILY_TOKEN_CAP config all removed as dead surface.

--- Step 7: OSS repo hygiene --------------------------------------------------
[x] 7.1  Add CONTRIBUTING.md (state current PR/issue posture honestly).
         Done — sets expectations (small focused PRs welcome, open an
         issue first for anything nontrivial, points at
         docs/project-gaps.md for what's worth working on). (2026-08-19)
[x] 7.2  Add SECURITY.md (private vulnerability reporting contact/process).
         Went with GitHub's private-advisory flow (no email published),
         per explicit choice when asked. Note: tried to enable "Private
         vulnerability reporting" via `gh api -X PUT .../private-
         vulnerability-reporting` — 404'd (feature likely needs to be
         turned on manually in Settings -> Security once the repo is
         public, or isn't available on this plan). SECURITY.md says so
         explicitly and gives a fallback (open a detail-free issue
         asking the maintainer to enable it). REMAINING MANUAL STEP:
         flip on Settings -> Security -> "Private vulnerability
         reporting" after go-public (step 9). (2026-08-19)
[x] 7.3  Set the GitHub repo description + topics via `gh repo edit`.
         Done — description + 7 topics (distributed-systems, docker,
         fastapi, llm, p2p, redis, self-hosted-ai) set and confirmed via
         `gh repo view`. (2026-08-19)

--- Step 8: README hero pass --------------------------------------------------
[x] 8.1  Add an above-the-fold hero to README.md: screenshot/GIF of the chat
         UI, one line on what the project demonstrates, and the live +
         demo-mode links, ahead of the architecture diagram.
         Done — added right under the tagline: a "Try it now" link to
         /demo, a real screenshot (docs/images/demo-page.png), and a
         paragraph naming what to actually look at (smart-mode 14B
         model-split across two machines, the invite/quota system,
         project-gaps.md as a maintained honest gap list).
         verify: booted coordinator+client in-process (fakeredis-backed,
         no Docker/Redis available in this sandbox) via a throwaway
         script, drove a real /demo/generate -> /jobs/next -> /jobs/
         complete round trip over HTTP, and captured the actual
         rendered page with headless Chrome — not a mockup. Full test
         suite re-run clean after the doc edit: 587/587. (2026-08-19)

--- Step 9: go public (manual confirmation gate) -------------------------------
[ ] 9.1  Flip the GitHub repo from private -> public.
         STOP: requires your explicit live go-ahead at the time — not done
         automatically as part of this list, even once every prior item is
         checked off.

--- Step 10: distribution -------------------------------------------------------
[x] 10.1 Write a 3-sentence LinkedIn/resume blurb (problem -> mechanism ->
         what's live today) with repo + live-demo links.
         Done — docs/blurb.md: a one-liner for a resume line, a ~3-
         sentence version for a LinkedIn post/portfolio card, and a
         note to swap wording once the repo is actually public (the
         GitHub link 404s for outsiders until step 9 happens).
         verify: content review only, as planned. (2026-08-19)
