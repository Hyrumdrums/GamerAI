# Test-only dependencies. Not installed into worker / coordinator images.
# Install with: pip install -r requirements-dev.txt
fakeredis==2.26.1
httpx==0.27.2  # FastAPI TestClient requires httpx
reportlab==5.0.1  # generates a real PDF fixture for tests/test_uploads.py
