consider markdownn strategy.

Utilize a full CI deploy, so no git keys need to be on server.

maybe revisit the read alloud feature, either dissable or go back to the line by line speach (or maybe exponentially increase the sentances sent so the first is read fast, and we have time to generate the rest).

admin link should only show to admins, though it's correctly showing a 403 when used by non admins
  - checked 2026-08-21: both places that render the admin link (account.html.j2
    server-side {% if me.role == 'admin' %}, and chat.js's adminlink.hidden
    toggle keyed on state.me.role) are already correctly gated. Couldn't find
    a code path where it shows unconditionally. Leaving this line in case it
    resurfaces in the field — if seen again, capture which page it appeared on.

DONE 2026-08-21: "All Workers" admin dashboard table only showed worker_id,
  couldn't tell which account owned which worker. Added an "Account" column
  (coordinator /workers now joins workers.owner_member_id -> members.username
  /email, dashboard.html.j2 renders it).

DONE 2026-08-21: search-toggle bug ("cool"/"hi"/"error?" came back as fully
  empty assistant replies). Root-caused via code read, not live repro:
  - The search checkbox auto-unchecking after "cool" was NOT a bug — it's
    an existing by-design feature (streamingEngine.js: res.search_was_skipped
    auto-unchecks the box when the rewrite classifier decides a follow-up
    doesn't need search, e.g. "That's cool!" after a news thread). The
    Sources shown FOR "cool" itself were the small 3B routing classifier
    imperfectly judging a short ack as needing a definition lookup
    (Wikipedia/Merriam-Webster/Cambridge hits) -- a model-accuracy edge
    case in _build_search_query_meta_prompt, not a wiring bug. Left alone;
    could tighten the meta-prompt's short-ack examples later if it recurs.
  - The REAL bug: windows-agent/agent.py's run_inference() streams from
    Ollama and only raises on an HTTP/transport failure. If Ollama's GPU
    inference dies mid-request (driver timeout/reset, OOM, crash — the
    GPU driver hiccup, unrelated to the sd.exe crash below which turned
    out NOT to be driver-related) it can return a clean
    HTTP 200 stream that goes straight to done:true with zero content
    tokens. process_one then reported that as status="complete" with
    text="", which the client renders as a bare "(empty)" bubble with no
    error styling and no retry button -- a silent, unactionable failure.
    Fixed: process_one now raises when result["text"] comes back blank,
    which routes through the existing except-Exception handler and reports
    status="error" instead — client shows "Generation failed." + a retry
    button, and the agent log gets a proper log.exception line. Applies to
    chat, search, and smart-mode jobs (same code path). Shipped in v1.3.6.

DONE 2026-08-21: image-gen crash on the GPU VPS test machine, sd.exe rc=
  3221226505 (STATUS_STACK_BUFFER_OVERRUN) loading dreamshaperXL-lightning
  .gguf. Root-caused with direct machine access (Remote Control session on
  the VPS) instead of guessing from a pasted log:
  - NOT a driver/VRAM issue. Windows System event log is completely silent
    in the crash window -- no TDR/driver-reset event fired. The "AMD
    Software" driver-timeout popup the user saw was coincidental.
  - Real cause, confirmed via WER (Event ID 1000/1001): sd.exe hit
    exception 0xC0000409 in ucrtbase.dll -- the CRT's /GS stack-cookie
    check killing the process because the GGUF loader corrupted its own
    stack while tallying per-tensor weight dtypes at model-load time
    (crashed before any generation/GPU work started). Looks like a fixed-
    size stack array overflowing on a model with more distinct quant types
    than expected -- dreamshaperXL-lightning's GGUF mixes f16 + q4_K.
  - This is a bug in the upstream stable-diffusion.cpp release binary
    (sd.exe is pulled prebuilt per infra/setup-image-mirror.sh, pinned to
    master-637-ef92a00 -- not built from source here, so not patchable
    directly), and dreamshaperXL-lightning is the one hardcoded default
    image model every Windows agent bootstraps -- not a one-VPS quirk.
  - Fixed (scoped, per go-ahead to "leave it for now" beyond this): agent.py
    now catches this specific rc and raises a clear message ("stack buffer
    overrun in the GGUF loader -- a known stable-diffusion.cpp bug ...
    not a driver or VRAM problem") instead of a raw rc + truncated stdout
    snippet. Revisit the model/binary swap only if this starts recurring
    more -- options on the table if so: pin a newer sd.exe release, swap
    to a uniformly-quantized (non-mixed-dtype) model variant, or file
    upstream with leejet/stable-diffusion.cpp.

DONE 2026-08-21: /register 401 token-mismatch mystery (state.json's token
  never matching the coordinator DB) recurred a second time on the same GPU
  VPS during a clean re-install flight test. Root-caused with direct machine
  access (a Claude session running on the VPS via Remote Control): the
  installer's postinstall "Launch now" checkbox + plain Start Menu/Desktop
  icons launch agent.exe with no --tray flag, and single-instance protection
  (_claim_single_instance) only ran for --tray launches -- so a plain console
  launch had zero protection against a second agent.exe process existing at
  the same time, and the two would silently clobber each other's state.json
  writes (including a just-issued signup token) with no coordination at all.
  Fixed: single-instance claiming now runs for any long-running launch, not
  just tray mode (still exempts --once/--status). Shipped in v1.3.7.


DONE 2026-08-21: "windows 11 doesn't hide in the tray like on pre-11".
  Root cause: Win11 defaults to Windows Terminal as the console host, and
  GetConsoleWindow() returns conhost's invisible ConPTY window in that
  case, not the visible WT tab -- so ShowWindow(SW_HIDE) hides a window
  nobody sees. Pre-11 (and Win11 with legacy console host set as default)
  works fine because GetConsoleWindow() there IS the visible window. No
  clean in-app fix exists (Win32 doesn't expose "which WT window is
  mine," and guessing risks hiding an unrelated terminal window of the
  user's) -- per owner's call, documented as a known limitation +
  workaround (switch default terminal app to Windows Console Host) in
  windows-agent/README_addendum.md instead of shipping a heuristic fix.
  Revisit with a real fix (consoleless CREATE_NO_WINDOW relaunch, trading
  away "Show console") if this keeps coming up.

DONE 2026-08-21: open signup abuse -- anyone could /signup with a
  throwaway, unverified email and immediately get a full BRONZE quota
  (100k tokens/day, 20 images/day, 30 voice min/day) with zero
  requirement to ever install/run the agent. Fixed with Resend-backed
  email verification (owner's choice over SMTP-via-personal-mailbox --
  free tier covers current volume, better deliverability, no risk to a
  real inbox):
  - members.email_verified (new column, defaults to 1 -- only signup
    rows are inserted with 0; invite/admin-created members are
    unaffected). New signup gates chat/image/voice submission (the
    /generate quota-check block) with a 403 until verified; contributing
    (running the agent as a worker) is NOT gated by this either way.
  - POST /signup generates a single-use code (redis, 24h TTL) and
    emails a /verify-email?code=... link via coordinator/email_send.py
    (Resend REST API, stdlib urllib, no new dependency). If
    RESEND_API_KEY is unset OR the send itself fails, the member
    auto-verifies instead of being locked out of an email that can
    never arrive -- same fallback used by the new POST
    /me/resend-verification for a lost/never-received email.
  - windows-agent/agent.py's run_signup_flow (the ONLY place /signup is
    called from -- there's no web signup form) used to synthesize a
    fake, unmailable placeholder email (@agent.gamerai.local) since
    nothing read the email field before now. Now prompts the console
    user for a real one (3 attempts, then aborts) and prints a
    "check your inbox" note when the signup response comes back
    unverified. Shipped in agent v1.3.8.
  - RESEND_API_KEY / EMAIL_FROM added to .env (local) and .env.prod (VPS,
    added by hand over SSH); PUBLIC_BASE_URL already derived from DOMAIN
    in infra/docker-compose.prod.yml, didn't need adding. Deployed live.
  - Tests: tests/test_member_auth_e2e.py::SignupEmailVerificationTests
    (6 new, Resend mocked, no real network calls) + updated
    tests/test_agent_bootstrap.py::RunSignupFlowTests for the email
    prompt. Full suite (592 tests) green at ship time.

DONE 2026-08-21: web signup flow + admin test-email tool + downloads-page
  copy actually deployed. Three asks bundled in one pass:
  - The downloads-page copy fix from earlier this session was edited
    locally but never committed/deployed -- it lives at
    /var/www/downloads-chroot/uploads/index.html on the VPS (copied
    there by infra/setup-mirror.sh), NOT inside a docker image, so the
    regular deploy.sh never touches it. Committed + copied it over by
    hand; confirmed live via curl.
  - Admin "send a test email" tool: dashboard.html.j2 now has an
    "Email delivery test" card (email input + button), wired through
    a new coordinator POST /admin/test-email (admin-only) ->
    email_send.send_test_email() -- a plain "this is a test" message,
    distinct from the verification-link template, so a real send
    during testing never looks like an account prompt. Surfaces the
    actual Resend error (bad key, domain not verified, etc.) instead
    of a bare pass/fail.
  - Web signup: new /join page (client/routes/signup.py +
    signup.html.j2 -- URL is /join, NOT /signup; see below), reachable
    from the landing page and the login page (both previously had
    stale invite-only copy). Posts to the coordinator's existing POST
    /signup, auto-logs in via session cookie same as invite-accept.
    Verification stayed LINK-based (the coordinator's existing GET
    /verify-email), not a typed code -- reused what was already built
    and tested rather than adding a parallel code-entry mechanism.
    account.html.j2 now shows a verified/not-verified badge + "Resend
    verification email" button (new POST /account/resend-verification)
    so the status persists past the first-signup moment, not just a
    one-time flash message.
  - Second bug, caught by the deploy itself: curl'ing the new page in
    prod returned the coordinator's bare 401 -- infra/Caddyfile routes
    an explicit path allowlist to the client container and falls
    everything else through straight to the coordinator, which already
    owns a public /signup (the Windows agent's console flow POSTs JSON
    there directly). A Caddy matcher pointing /signup at the client
    would have hijacked the agent's API calls behind a form-encoded
    handler expecting multipart data -- silently broken every agent
    signup in the field. Renamed the browser page's URL to /join
    (kept the signup.py/signup.html.j2 filenames -- that's the
    feature name, /join is just the route) and added the matching
    Caddy block. No test suite could have caught this since it's a
    routing-layer conflict, not an app-layer one.
  - Bug caught mid-build, fixed before touching prod: main.py already
    had its own PUBLIC_BASE_URL (agent-pairing section) that silently
    shadowed the shared.config import I'd added for the verify-email
    link -- an unconfigured deploy would have emailed a bare
    "/verify-email?code=..." with no host. Fixed to reuse the existing
    var with the same request.base_url fallback pairing already uses;
    regression test added (asserts the emailed link is absolute).
  - Verified end-to-end against the real local dev stack (not just
    coordinator-side unit tests): screenshotted /signup, /login,
    landing, account (unverified badge + resend), and the dashboard's
    test-email card (both the missing-key red-error state and the
    happy-path green state) via Playwright against tools/run_local.py.
  - Tests: 6 new tests in tests/test_web_ui_smoke.py covering the
    signup page, auto-verify vs. gated-with-flash paths, duplicate
    username, resend, the verified/unverified badge, and the admin
    test-email tool (success, misconfigured, non-admin 403). Full
    suite: 606 passed.

refactor god files like main.py... 4k+ lines

DONE 2026-08-22: double-checked "usage only allowed once agent is
  running" -- audited the code path + verified live on prod rather
  than just reading it:
  - coordinator/main.py's /generate calls _ensure_live_worker_or_503()
    (gated by REQUIRE_LIVE_WORKER) BEFORE the quota check and before
    any DB writes -- a submission with no live worker 503s with no
    job queued and no usage row touched. Token/image/voice usage is
    only ever credited from /jobs/complete (db.add_member_usage),
    which structurally cannot fire without a worker actually claiming
    and completing the job -- so there's no path where usage gets
    debited without an agent having run. Already covered by
    test_coordinator_e2e.py's three REQUIRE_LIVE_WORKER tests (503 +
    empty queue when no worker, 200 when one is registered, gate
    no-ops when disabled).
  - The part worth actually checking live: is the gate ON in prod, or
    just a dev-only toggle that happens to default false? Confirmed
    via SSH: /opt/gamerai/.env.prod has REQUIRE_LIVE_WORKER=true and
    it's live in the running gamerai-coordinator container's env; the
    mock-worker compose profile is NOT up (no fake in-VPS worker to
    silently answer everything). Read worker_heartbeats straight out
    of prod Redis and found real win-*-prefixed worker IDs
    heartbeating within the last ~15-20s at check time -- i.e. an
    actual Windows agent was live and would have unblocked /generate
    for real, not just in theory.
  - Couldn't complete the loop with an actual curl'd /generate
    against prod as "proof" -- the admin API_TOKEN in .env.prod no
    longer authenticates (by design: it stops matching any member
    once the founding admin sets a real username/password, see
    ensure_admin_seed()'s docstring), and I'm not going to ask for or
    use the account password. If you want the literal "send a prompt,
    watch it go through" check, that's a 30-second manual pass at
    https://ai.dallinlayton.com/ under your own login -- everything
    else about the gate is now verified end-to-end.
  - Side observation, not acted on: worker_heartbeats in prod Redis
    has a long tail of stale entries (~100+ days old, mostly
    non-win-prefixed "worker-xxxx" dev/test IDs) that nothing ever
    prunes -- _worker_status() correctly ignores them for online/
    offline classification, so this isn't a correctness bug, just
    unbounded hash growth. Worth a cheap cron/reaper if it's ever
    noticeably large; not touched here since it's outside what was
    asked.

DONE 2026-08-23: closed out a low-hanging-fruit / gaps review session.
  Bundling the summary here since todo.txt is where open work actually
  lives (retired the one-off project-review-todo.txt checklist file
  after merging its still-open items into the backlog block below).
  - Found docs/project-gaps.md's "2026-05-13 security review" section
    (6 items, half marked critical) was entirely stale -- all six were
    already fixed in code, the doc just never got updated. Verified
    each against actual code, not commit messages: canary leak
    (main.py:1990/:2434 drop submitted_by_member_id from the worker
    envelope), worker/member spoofing (owner_member_id enforced,
    main.py:2786/:2709), markdown XSS (DOMPurify in
    messageRenderer.js), CDN SRI (moot -- marked.min.js is self-hosted
    now), prompt-length cap (MAX_PROMPT_BYTES, shipped 2026-08-19),
    /login rate limit (LOGIN_FAIL_MAX, on by default). Also fixed the
    membership-identity "remaining polish" list (3 of 5 bullets were
    actually done -- worker/member link, tier auto-promotion engine,
    per-member web UI auth; one was superseded not done -- Caddy
    basic_auth, replaced by session-cookie auth instead; confirmed
    SMTP-delivered invites is still genuinely open) plus a stale line
    in the 30-day priority list.
  - Also did a fresh-eyes pass over the rest of the codebase for
    low-hanging fruit and gaps not already tracked anywhere -- see the
    backlog block immediately below for what came out of that.

--- Feature/idea backlog (from the 2026-08-23 review, unordered) ----------

Leaderboard: README.md promises "tier badges on the leaderboard"
  (~line 335) -- doesn't exist, grepped repo-wide. Data's already
  collected (members.tier, the 7-day uptime window tier_engine.py
  already aggregates, jobs-served counts) -- rank by tier/uptime, NOT
  $ earnings (consistent with fc8aa28 dropping the misleading earnings
  display). Reuse tier_engine.py's query shape. Bonus: a "current
  streak" stat is ~free on the same data. Pairs well with the
  tier-history table below so promotions can show "promoted 3 days
  ago". ~half a day.

Tier-change + invite-accepted push notifications: coordinator/
  events.py (the new in-process event bus) and coordinator/
  notifications.py (send_to_member, opt-in prefs, push delivery) both
  already exist but were never connected to each other for
  member-facing notifications -- only the admin-alert emailer
  subscribes to the event bus today. tier_engine.py:290 and :337
  already log member_promoted/demoted with zero user-facing signal;
  main.py:4744/:5407 already emit on invite accept. Wiring either to
  send_to_member with a new category is ~15-30 min once the first one
  establishes the pattern.

503-loses-your-message composer bug: fully root-caused already in
  docs/project-gaps.md (search "A 503 silently destroys the user's
  message") -- don't clear the textarea until the fetch succeeds,
  restore the prompt text on any error path, add a retry button.
  client/static/js/chat.js, client-only fix. ~1-2 hours.

Uptime Kuma (or equivalent) hitting /health every 30s -- the only real
  monitoring gap between "site's down" and "a user tells you it's
  down". Already scoped in project-gaps.md §2 / §7 item 6. ~half a
  day.

Supply/demand push nudge to idle contributors: per-tool Redis queues
  (job_queue_for() in shared/config.py) + the event bus + push infra
  all already exist independently; nobody's wired "the image queue is
  backed up" into a push aimed at idle image-capable contributors. GET
  /metrics (main.py:5642) only reports the legacy chat queue depth
  today -- needs to loop over every job_queue_for() tool name first,
  then a periodic threshold check (mirrors canaries.py's interval-task
  pattern). ~1 day.

Live power-cost / earnings-equivalent stat in the agent tray: makes
  README §5's $/kWh economics pitch personal and real-time instead of
  a static table. Agent already reports GPU model/VRAM (faef05c) --
  poll `nvidia-smi --query-gpu=power.draw` and show "earned the
  network N tokens today, ~$0.0X in power" in the tray/console.
  windows-agent/agent.py. ~1 day.

Community model-request wishlist: coordinator/model_registry.py is
  maintainer-curated; a public "I wish X model were on the network"
  page (one table + one page) turns curation into a bottom-up signal,
  fits the community-powered framing, doesn't obligate building
  support for whatever gets requested. ~half a day.

Turn the incident write-ups in this file into a devlog/blog series --
  the sd.exe STATUS_STACK_BUFFER_OVERRUN root-cause above, the Win11
  ConPTY tray-hide investigation, the TOCTOU stowaway-instance race --
  already written at near-publishable quality. public-launch-todo.txt
  step 10.1 (docs/blurb.md) shows the portfolio angle is already a
  goal; this is close to free content. No code, ~a few hours/post.

Tier-change history table: checked every CREATE TABLE in
  coordinator/db.py -- no promotion/demotion history exists. The only
  record is a structured log line, wiped on container restart (see
  project-gaps.md's own "no log retention" gap). Add
  member_tier_events(member_id, from_tier, to_tier, at), written from
  tier_engine.py's promote/demote path (~line 290/337 -- same spot as
  the push-notification hook above, worth doing together). Also feeds
  the leaderboard's "promoted 3 days ago" badge. ~2-3 hours.

DONE 2026-08-24: document/file upload (PDF, DOCX, TXT/MD, CSV) for
  chat context -- was already scoped in docs/project-gaps.md ("No
  document upload"); shipped per the owner's storage steer (raw file
  never touches disk, only extracted text persists) plus a few
  design calls made while building:
  - coordinator/uploads.py (new module, mirrors notifications.py's
    build_router(db)-closure shape): extract_text() dispatches by
    filename extension (pypdf / python-docx / plain decode / pandas
    for csv -- pandas gets a compact "columns + row count + 20-row
    preview" description, deliberately NOT full-fidelity data access;
    real computation over a CSV needs a code-execution sandbox, out
    of scope here same as the original doc's cut). Parses straight
    off the in-memory upload bytes -- nothing is ever written to a
    named file on the coordinator. New `uploads` table in
    coordinator/db.py stores only the extracted text, same
    plaintext-at-rest retention as jobs.prompt/messages.text (Phase
    3b.iii encryption will cover both together, no separate carve-out
    invented for this).
  - Two caps, both truncate-with-an-explicit-marker rather than
    silent drop: MAX_UPLOAD_EXTRACTED_CHARS per file (20k default,
    applied at extraction) and MAX_UPLOAD_CONTEXT_CHARS combined
    across every upload folded into one <<document>> fence (30k
    default, newest-file-first so a just-attached file always makes
    the cut even when older ones in the same conversation don't).
    MAX_UPLOAD_BYTES (5MB default) caps the raw upload itself.
  - Wired into coordinator/main.py: POST /uploads and GET
    /uploads/{conversation_id} (member-auth + conversation-ownership
    gated, same 404-not-403 convention as everything else conv-
    scoped). _build_chat_messages_with_info() gained a
    document_context param, folded in as a system message right
    before the current turn (not up top with the rolling summary --
    wanted the model's attention right next to the question it's
    for). Sticky per conversation: every /generate for tool=chat
    re-fetches that conversation's uploads, so a document attached on
    turn 1 is still in context on turn 5 with no re-upload. Also
    wired into POST /messages/{id}/retry, which had its own separate
    context-rebuild path -- missed on the first pass, caught because
    the test suite below covers it explicitly.
  - Client: paperclip button + hidden file input in the composer
    (client/templates/chat.html.j2), new client/static/js/
    attachments.js module (upload, render removable-looking chips,
    pending/failed states), wired into chat.js at the three lifecycle
    points that matter (init, "+ New chat" clears chips, opening/
    switching a conversation reloads them from the server so a page
    refresh doesn't lose the chip row). Extracted the inline "create
    a conversation if this is a brand-new chat" block that used to
    live only in chat.js's submit handler into a shared
    ensureConversation() in state.js, since attaching a file before
    the first message needed the exact same lazy-create behavior
    submit already had -- one shared helper instead of a second copy.
    client/services/api_client.py gained proxy_upload() (multipart
    forwarding; httpx computes its own boundary header, don't set
    Content-Type by hand) alongside the existing proxy_json/proxy_raw.
  - New deps: pypdf, python-docx, python-multipart in coordinator/
    requirements.txt. pandas too, pinned to 2.3.3 (last 2.x release)
    rather than the current 3.0.5 -- pandas 3.x requires Python
    >=3.11 and the local dev .venv is 3.10 even though the Docker
    image is 3.11; only stable read_csv/head/to_string/columns APIs
    are used here so there's no functional reason to force the newer
    major. reportlab added to requirements-dev.txt (test-only) to
    generate a real PDF fixture rather than mocking pypdf.
  - Tests: tests/test_uploads.py, 27 new cases -- extraction unit
    tests against real generated PDF/DOCX/CSV fixtures (not mocks),
    the <<document>> fence budget/truncation/ordering logic, router
    auth/ownership/size/type validation, and integration tests that
    inspect the actual queued job_queue envelope to confirm
    document_context lands in messages[] on both /generate and
    /messages/{id}/retry, and does NOT leak in when nothing's
    attached. Full suite: 659/659 green.
  - Verified live, not just unit-tested: ran tools/run_local.py and
    drove the real browser UI with Playwright (login via the dev
    bearer-token shortcut, attach a .txt through the actual file
    picker, watched the chip go pending -> settled, sent a message,
    confirmed "+ New chat" clears chips and switching back to the
    conversation reloads them, zero console errors) -- then cross-
    checked the dev SQLite DB directly and confirmed the extracted
    text matched the uploaded file exactly.
  - Bug caught by the test run, not by review: the upload-extracted
    log.info() call used "filename" as an extra= key, which collides
    with LogRecord's own reserved `filename` attribute (the source
    file of the log call itself) and raised KeyError at log time on
    every successful upload. Renamed to "upload_filename".
  - Not deployed -- explicitly told to skip that this session. Bumped
    CLIENT_CACHE_VERSION anyway (23 -> 24) since the diff touches
    client/static/**, so whenever this does ship the PWA cache-bust
    is already correct and nobody has to remember it later.
  - Noticed, not touched: README.md's own §10 API table doesn't list
    /uploads (or /conversations, /invites, /login, /me, and roughly
    a dozen other real endpoints) -- it's stale from around the
    original MVP and was never kept current as membership/
    conversations/invites shipped. Predates this change and is a
    much bigger cleanup than this slice; flagging here rather than
    scope-creeping into a full README API audit.

DONE 2026-08-24: image alteration (img2img) on an uploaded image,
  agent side. Scope cut deliberately to plain img2img -- masking/
  inpainting needs a paint-a-mask UI, a bigger separate lift, not
  built here (see "not done" list below).
  - Nailed down the exact CLI flags before touching agent.py, since
    the only verification path available (no Windows box / real
    sd.exe here) was upstream's own source at the pinned commit
    (master-637-ef92a00): fetched examples/common/common.cpp and
    found the real option-registration table -- `-i`/`--init-img`
    for the init image, `--strength` ("strength for noising/
    unnoising", default 0.75), `--mask` for a mask image (unused --
    see above). Also confirmed via resolve_and_validate() that
    --init-img is OPTIONAL for -M img_gen mode (only UPSCALE/
    ADETAILER modes require it) -- so the existing txt2img invocation
    just gains flags when an init image is present, no mode switch.
    And confirmed via load_image_and_update_size() that -W/-H still
    apply with -i set (sd.cpp resizes the init image to them, doesn't
    require an exact match) -- so no special-casing needed there.
  - coordinator: ImageParams gained init_image_b64 + strength
    (shared/models.py). New _validate_and_classify_init_image()
    (coordinator/main.py) gates it before the job ever reaches a
    queue -- size cap (reuses MAX_IMAGE_BYTES), base64 shape,
    PNG-or-JPEG magic header, AND the same NudeNet NSFW classifier
    generated OUTPUT images already get (_classify_image_or_raise,
    now called from both directions) -- an edit job hands a
    contributor's GPU someone else's uploaded picture, didn't want
    that getting a weaker gate than a generated one. strength
    clamped to (0, 1]. tool stays "image", no new capability/queue --
    any worker already advertising tools=["image"] is already running
    the sd.exe that supports this.
  - agent: run_image_inference() (windows-agent/agent.py) decodes
    init_image_b64, writes it next to the existing prompt-temp-file
    (out_path.with_suffix(".init.png")), adds -i/--strength to argv
    when present, cleans up in the same finally block as the prompt
    file (including on subprocess failure). strength omitted from
    argv (not defaulted) when the job didn't send one -- sd.exe's own
    0.75 applies, matches this file's standing rule against
    re-deciding model defaults agent-side. AGENT_VERSION -> 1.4.1.
  - client: new "Edit an image" composer toggle, 4-way mutually
    exclusive with generate/search/smart now (composer.js's
    3-way pairwise-condition mutual exclusion got refactored to an
    array-driven TOOL_TOGGLES loop rather than hand-writing a 4th
    copy of the same logic). File picker + subtle/moderate/strong
    strength preset; submit base64-encodes the picked file
    client-side (chunked, to avoid blowing the call stack on a
    multi-MB photo) and sends it with no width/height pin -- edit
    mode keeps the source image's own dimensions rather than forcing
    a generate-mode size preset. Validates a file was actually picked
    before touching the chat pane, not after.
  - Tests: tests/test_agent_image_edit.py (6 cases) mocks IS_WINDOWS
    + sd.exe/model paths + subprocess.run to verify the actual argv
    construction -- txt2img unaffected when no init image, -i/
    --strength present and correctly valued when one is, cleanup
    happens even on subprocess failure. tests/test_image_edit_e2e.py
    (8 cases) covers the coordinator side: envelope forwarding,
    strength clamping, oversize/bad-base64/bad-magic-bytes rejection,
    and NSFW-wiring (classifier mocked to raise -- NOT testing
    NudeNet's real detection accuracy, same precedent
    test_coordinator_e2e.py already set for that). Full suite:
    673/673 green.
  - HONEST GAP: the real sd.exe subprocess was never actually run --
    couldn't be, no Windows box here. Every agent-side test mocks
    subprocess.run; the argv-construction logic is what's verified,
    not that sd.exe accepts it and produces a sane edit. First real
    contributor-hardware test is the thing that closes this out.
  - Verified live (the part that COULD run here): Playwright against
    tools/run_local.py -- 4-way toggle exclusivity, the "choose an
    image first" validation path (no optimistic bubble leaks on
    failure), a real file pick + strength selection + submit getting
    a 200 back, composer reset after submit. Zero console errors.
  - Not done: masking/inpainting (--mask, needs a paint-a-mask UI),
    chaining from a previously-generated image in the same
    conversation (the PNG already exists on disk at /data/images/ --
    cheap data-wise, real UI lift), WEBP input.
  - Not pushed -- windows-agent/agent.py changes auto-fire the
    windows-agent-build CI workflow on push to main (CLAUDE.md §2);
    holding off given the untested-on-real-hardware gap above.

vision (image understanding in chat, agent side): already scoped in
  docs/project-gaps.md ("No image input (vision) on chat") -- called
  out there as the single most-cited capability gap vs. ChatGPT/
  Copilot/Gemini. Unlike image alteration above, this genuinely is a
  new tool: a VLM (Llama 3.2 11B Vision class) most contributors don't
  have, its own agent bootstrap/download flow, its own `chat-vlm`
  capability advertisement + queue, VRAM-gated eligibility the way
  SDXL already is for image-gen. Budget closer to a full week once
  agent-side bootstrap + real-hardware testing is counted, not the
  doc's optimistic ~2-3 days. Do this one standalone, after doc
  upload and image alteration land -- biggest perceived-IQ win but
  the most expensive of the three, and it doesn't share the
  "attach a file" composer plumbing the other two do until the UI
  stage.

DONE 2026-08-24: real-hardware confirmation on image alteration --
  owner tested live: agent 1.4.1 self-updated correctly, "Edit an
  image" round-tripped through a real sd.exe img2img job. Closes the
  "honest gap" noted when this shipped (subprocess.run was mocked in
  every test since there was no Windows box available). Two follow-up
  asks from that test, both shipped same day:
  - Picked-image thumbnail: composer.js's file picker used to show
    only a filename. renderEditThumb() now decodes via
    createImageBitmap + draws a cover-cropped 64x64 canvas ->
    dataURL(JPEG, q0.7) -- an actual small re-encoded image, not a
    CSS-shrunk full-res one, so this stays cheap on a multi-MB phone
    photo. A monotonic _thumbToken guards against a slow decode from
    an earlier pick landing after a newer one supersedes it.
  - Chain-edit: the lightbox (imageGallery.js) gained an "Edit"
    action next to Save. Fetches the currently-viewed PNG from the
    same /api/images URL the <img> already used (same ownership
    check, no new surface), wraps it as a File, hands it to
    composer.js's new setPickedEditFile() -- which flips the
    composer into edit mode via a real dispatched 'change' event
    (not a direct .checked assignment) so the existing mutual-
    exclusion/persist/refresh handler runs unchanged, one code path
    for both "user picked a file" and "reusing a generated one".
    Deliberately NOT a per-bubble button -- the lightbox already had
    a clean action-row pattern (Save) to extend, and putting Edit
    there avoids cluttering every image bubble in the log.
  - Verified locally (Playwright against tools/run_local.py): thumb
    appears as a real data: URL (not hidden) on file pick and clears
    on mode-exit; a stand-in <img class=generated> (the local harness
    has no image-capable worker, so this used a real static PNG the
    app already serves rather than an actual generation) opens the
    lightbox, Edit closes it and correctly flips edit mode on with
    the right filename + thumbnail. Zero console errors. Full Python
    suite unaffected (pure client-side change): 673/673.
  - CLIENT_CACHE_VERSION 25 -> 26.

DONE 2026-08-25: PWA update-propagation fix -- root cause of a second
  round of "the feature I just shipped isn't showing up" reports.
  - Owner reported (after the thumbnail/chain-edit deploy above): no
    thumbnail on desktop file-pick, no Edit button in the lightbox on
    desktop, and on mobile the "Edit an image" checkbox does nothing
    at all (no error, edit-controls never reveal). Browser's version
    chip read v1.0.26 -- the current build -- which looked like it
    ruled out staleness.
  - It didn't. v1.0.26 is server-rendered straight from
    shared.config.CLIENT_CACHE_VERSION on every navigation (network-
    first per sw.js's fetch handler), so the chip always reflects
    what the SERVER is running, not what the ACTIVE service worker
    has cached. A tab/PWA stuck on an old SW shows the new number
    while every cache-first JS fetch (composer.js, imageGallery.js)
    still resolves to whatever SHELL_CACHE that old SW populated --
    exactly the false-negative this session's earlier "checkbox does
    nothing" bug already hit once, now resurfaced one deploy later.
  - Confirmed it wasn't a real code regression first: curled prod and
    diffed composer.js/imageGallery.js byte-for-byte against the repo
    (identical), grepped chat.css's mobile breakpoint for anything
    that could hide .edit-controls/.iv-edit (nothing does -- only
    .iv-nav prev/next arrows are mobile-hidden). Server side was
    already correct; the gap was purely client-side propagation.
  - Fix: client/templates/base.html.j2's SW registration (the one
    script block every page loads, not just chat.html.j2) now calls
    registration.update() on load, on visibilitychange->visible, and
    every 20 min on a timer, instead of relying solely on the
    browser's own heavily-throttled background check. On
    updatefound -> installing.statechange === 'installed' with an
    already-existing controller (i.e. a real update, not a first-ever
    install), it shows a small "A new version of GamerAI is ready"
    banner with Refresh/Not now, reusing the existing .pwa-banner
    CSS (same class the install/push-opt-in banners already use --
    zero new CSS). Not auto-reloading on its own: this app has no
    draft-preservation for an in-flight message, so silently yanking
    the page out from under a mid-type or mid-stream user would trade
    one bug for a worse one.
  - This fix reaches every client on their next page load with zero
    SW dependency (HTML navigations are network-first, so the new
    inline script itself can't be stuck behind the exact staleness
    problem it's fixing) -- and because CLIENT_CACHE_VERSION (26) is
    unchanged, it also resolves the owner's CURRENT stuck state: the
    next load fetches the new base.html.j2, immediately calls
    reg.update(), finds their old SW's script byte-differs from the
    live v26 one, installs+activates it (skipWaiting/clients.claim
    already unconditional), and prompts Refresh -- no DevTools
    unregister instructions needed this time.
  - Verified locally (Playwright against tools/run_local.py): no
    spurious banner on a normal load; window.showUpdateBanner (a bare
    function decl in the inline script, so it lands on window for
    free) renders the correct text/buttons, a second call doesn't
    duplicate it, Not now removes it, Refresh triggers a real
    navigation. Zero console errors. Full Python suite unaffected
    (template + inline JS only, no backend touched): 673/673.
  - Template-only change -- no client/static/** files touched, so no
    CLIENT_CACHE_VERSION bump needed (see CLAUDE.md's cache-bust
    gotcha: it's scoped to client/static/**, and this is the one kind
    of client change that's exempt by construction).
  - HONEST GAP: no automated test exercises the real two-SW-versions
    lifecycle (would need a second live deploy mid-test to produce an
    actual updatefound event) -- the banner's own rendering/dismiss/
    refresh mechanics are verified directly instead. Also: a tab that
    is never revisited and never regains focus still won't update
    until closed and reopened -- this narrows the staleness window
    from "indefinite" to "one page load," not to zero.

DONE 2026-08-25: generated-image HTTP caching + global error banner --
  two more reports right after the update-banner fix above.
  - Owner: "images don't seem to be cached, on every page navigation
    it slowly loads in a few dozen pixel rows at a time." Root cause
    (not a caching-propagation issue this time, a real missing-header
    bug): client/services/api_client.py's proxy_raw() -- the only
    thing that serves /api/images/<name> to the browser -- built a
    brand-new Response from just content/status_code/media_type,
    dropping every header the coordinator's FileResponse would set
    (ETag, Last-Modified, everything). No Cache-Control, no
    validators -- the browser had zero basis to reuse a fetched image
    across navigations, so every page load re-downloaded and re-
    decoded every image in the thread from scratch (the row-by-row
    paint is just that decode happening live over the network instead
    of instantly from disk cache). sw.js was never in play here --
    /api/* is deliberately network-only in the fetch handler (correct
    for JSON, wrong for an immutable binary hiding under that prefix).
  - Fix: proxy_raw() gained an opt-in `cache_control` param (opt-in,
    not default-on -- most /api/* bodies are per-request JSON that
    must never be cached, so this only applies where a caller
    explicitly says its bytes are immutable). /api/images/{name}
    passes "private, max-age=31536000, immutable" -- private because
    it's a per-session proxied fetch, not something a shared cache
    should store; immutable+1y because a given job's PNG is written
    once and never changes. New test:
    test_api_images_proxy_sets_immutable_cache_control in
    test_web_ui_smoke.py (writes a fake PNG straight into
    coordinator.main.IMAGE_DIR, hits the proxy as admin -- admin
    bypasses the ownership walk in coordinator/main.py's serve_image,
    so no conversation/job fixture needed -- asserts the header and
    that bytes round-trip unchanged).
  - Owner also, same message: after fully deleting + clearing the
    installed PWA's app data on mobile, the attach (document upload)
    button still does nothing and the "Edit an image" checkbox still
    does nothing -- no errors. A full data clear rules out stale-SW
    as the explanation (that's a clean-slate install), which the
    previous two mobile reports in this session could plausibly still
    have been.
  - Investigated hard before guessing again: curled prod and diffed
    composer.js/imageGallery.js byte-for-byte against the repo (still
    identical -- server-side is correct); re-read attachments.js top
    to bottom (no exotic APIs, nothing mobile-unsafe); ran the same
    checkbox+file-picker flow under Playwright with Chromium's
    iPhone-13 device emulation (real mobile viewport, touch taps via
    page.tap, mobile UA) -- both features worked with zero console
    errors, which rules out a generic touch-vs-click or narrow-
    viewport bug. Tried to go one step further and reproduce under
    real WebKit (Safari's engine) via Playwright, since that's the
    one variable the Chromium test can't cover -- blocked on two
    missing system libs (libevent-2.1-7, libavif13) that need sudo
    apt-get and this box has no passwordless sudo, so left untested
    rather than guess.
  - Given three straight mobile reports of "nothing happens, no
    errors" with no way to pull a real console log off the owner's
    phone, added a standing diagnostic instead of one more guess:
    base.html.j2 now has a global window.onerror +
    unhandledrejection handler that renders any UNCAUGHT error as a
    visible on-page banner (red, reuses the .pwa-banner shape) with
    the message + file:line, so a future silent failure becomes a
    screenshot instead of a dead end. Only fires for what the app's
    own try/catch and .catch() didn't already handle, and filters out
    bare cross-origin "Script error." (browser extensions / injected
    third-party scripts, no real detail available for those anyway).
    Verified via Playwright: no false-positive on a clean load, a
    real thrown error and a real unhandled rejection both surface
    correctly with the right text, second error while one's showing
    doesn't duplicate it, dismiss works.
  - Still open / needs the owner: what mobile OS + browser this is on
    (materially changes both diagnosis and fix -- e.g. iOS home-
    screen PWAs have a long-documented WebKit quirk where "delete the
    icon" does NOT reliably clear the underlying site data the way
    Android's Settings > Clear Data does; the real fix there is
    Settings > Safari > Advanced > Website Data, a separate, non-
    obvious step). Asked; waiting on a reply, ideally with the new
    error banner's text if it fires on retry.
  - CLIENT_CACHE_VERSION 26 -> 27 (base.css changed -- unlike the
    update-banner fix above, this one DOES touch client/static/**).
  - Full suite: 674/674 (added the one images-cache-header test;
    everything else unaffected).

DONE 2026-08-25: visited-link CTA buttons fading to plain-link blue --
  pre-existing bug, unrelated to the two commits above (owner asked
  "did we overengineer anything in those" while reporting this
  separately). base.css's top-level `a:visited { color: var(--brand) }`
  (specificity 0,1,1) outranks `.btn`'s `color: #fff` (0,1,0), so any
  visited .btn/.btn-quiet anchor lost its button styling and read as a
  plain visited link. Landing page has four (Try the demo, Download
  the agent, Create an account, View on GitHub); topbar's
  .contribute-cta had the same latent gap. Added the missing
  `:visited` pairs to both base.css and its mirrored copy in
  shared/ui.py (BASE_CSS, used by /tos -- the two are meant to stay in
  sync per the comment already at the top of base.css). Verified for
  real with Playwright: screenshotted the landing page, actually
  clicked through to /demo (real browser visited-state, not a
  simulated one -- getComputedStyle() lies about :visited for privacy
  so only a real paint proves it), navigated back, screenshotted again
  -- button stayed white-on-blue both times. CLIENT_CACHE_VERSION 27
  -> 28. No new automated test (CSS-cascade-only fix, and browsers
  deliberately can't be probed for :visited state from JS -- a
  permanent test here would just be a fragile screenshot diff for
  something an eyeball check already settled). Full suite unaffected:
  674/674.

DONE 2026-08-25: gated the uncaught-error banner to admin only -- owner
  flagged it (asked "did we overengineer anything in those last two
  commits") as the one piece worth reconsidering: it shipped to 100%
  of traffic, so a benign uncaught error from a vendored lib would now
  show ordinary members a scary "Something went wrong" they can't act
  on, in exchange for a debugging convenience that only helps
  development. Agreed, kept the mechanism (still the only real lead on
  the open mobile attach/edit bug) but scoped it: base.html.j2 fetches
  /api/me once on load (mirrors the pattern chat.js already uses for
  #adminlink, just without its redirect-to-/login-on-401 since base.html.j2
  loads on anonymous pages too) and the error/unhandledrejection
  listeners no-op unless role === 'admin'. Verified both directions
  live against tools/run_local.py: the admin dev-token session still
  gets the banner; a freshly signed-up member (role=contributor) sees
  nothing for the identical thrown error. Template-only change, no
  CLIENT_CACHE_VERSION bump needed. Full suite: 674/674.