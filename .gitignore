__pycache__/
*.pyc
*.pyo
.venv/
.env
.DS_Store
*.log
*.db
*.db-journal
*.db-wal
*.db-shm
data/
ollama-models/
.env.prod
# VAPID private key + any other ops secrets the coordinator reads
# at startup via VAPID_PRIVATE_KEY_PATH etc.
.secrets/

# windows-agent build outputs — produced by CI (and locally if you run
# build.bat). version.txt is generated at build time and bundled into
# agent.exe by PyInstaller; never committed.
windows-agent/version.txt
windows-agent/dist/
windows-agent/build/
windows-agent/Output/
windows-agent/*.spec
windows-agent/upload/
